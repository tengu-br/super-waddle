language: minimal

services:
  - docker

branches:
  only:
    - master

script:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker build -t superwaddle/proxy:$TRAVIS_BRANCH .
  - docker push superwaddle/proxy:$TRAVIS_BRANCH